package models

// model objects
var (
	UserModel        = new(User)
	TeamModel        = new(Team)
	TransactionModel = new(Transaction)
	TeamMemberModel  = new(TeamMember)
	SkillModel      = new(Skill)
)
