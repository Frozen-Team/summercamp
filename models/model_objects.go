package models

// model objects
var (
	UserModel        = new(User)
	TeamModel        = new(Team)
	TransactionModel = new(Transaction)
	TeamMemberModel  = new(TeamMember)
	SphereModel     = new(Sphere)
)
